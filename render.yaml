services:
  - type: web
    name: abujal-service
    env: dotnet
    rootDir: AbujalWebsite
    buildCommand: "dotnet publish -c Release -o output"
    startCommand: "dotnet output/AbujalWebsite.dll --urls=http://0.0.0.0:$PORT"
    plan: free